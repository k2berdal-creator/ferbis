<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>FERBİS - K2B</title>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<style>
*{box-sizing:border-box;margin:0;padding:0;}
body{font-family:'Segoe UI',sans-serif;background:#1a1a2e;min-height:100vh;display:flex;align-items:center;justify-content:center;}
.app{width:100%;max-width:1340px;height:96vh;background:#f0f0f0;display:flex;flex-direction:column;border-radius:8px;overflow:hidden;box-shadow:0 20px 60px rgba(0,0,0,.5);}
.topbar{background:linear-gradient(90deg,#880e4f,#c2185b);color:#fff;padding:5px 12px;display:flex;align-items:center;justify-content:space-between;flex-shrink:0;}
.brand{font-size:16px;font-weight:700;letter-spacing:1px;}
.topmenu{display:flex;gap:2px;}
.topmenu span{cursor:pointer;padding:4px 10px;border-radius:3px;font-size:12px;}
.topmenu span:hover{background:rgba(255,255,255,.2);}
.uinfo{font-size:11px;opacity:.85;display:flex;align-items:center;gap:8px;}
.rbadge{background:rgba(255,255,255,.2);padding:2px 8px;border-radius:10px;font-size:10px;}
.body{display:flex;flex:1;overflow:hidden;}
.left{width:190px;background:#fff;border-right:1px solid #ddd;display:flex;flex-direction:column;flex-shrink:0;}
.lsearch{padding:7px 8px;border-bottom:1px solid #eee;}
.lsearch input{width:100%;padding:5px 8px;border:1px solid #ddd;border-radius:4px;font-size:12px;}
.left h4{background:#880e4f;color:#fff;padding:5px 10px;font-size:11px;font-weight:600;text-transform:uppercase;}
.flist{flex:1;overflow-y:auto;}
.fi{padding:6px 10px;font-size:12px;cursor:pointer;border-bottom:1px solid #fafafa;line-height:1.3;}
.fi:hover{background:#fce4ec;}
.fi.active{background:#c2185b;color:#fff;}
.center{flex:1;display:flex;flex-direction:column;overflow:hidden;}
.fbar{background:#c2185b;color:#fff;padding:7px 14px;font-size:15px;font-weight:600;flex-shrink:0;}
.tabs{display:flex;background:#fff;border-bottom:2px solid #c2185b;overflow-x:auto;flex-shrink:0;}
.tabs::-webkit-scrollbar{height:3px;}
.tab{padding:7px 12px;font-size:12px;cursor:pointer;white-space:nowrap;border-bottom:2px solid transparent;margin-bottom:-2px;color:#666;}
.tab:hover{background:#fce4ec;color:#c2185b;}
.tab.active{border-bottom-color:#c2185b;color:#c2185b;font-weight:600;}
.tab.off{opacity:.35;cursor:not-allowed;pointer-events:none;}
.content{flex:1;overflow-y:auto;padding:10px 12px;}
.right{width:120px;background:#880e4f;display:flex;flex-direction:column;gap:3px;padding:6px 5px;flex-shrink:0;}
.rb{background:#fff;color:#880e4f;border:none;padding:7px 4px;border-radius:4px;font-size:10px;font-weight:700;cursor:pointer;text-align:center;line-height:1.3;}
.rb:hover{background:#fce4ec;}
.rb.gr{background:#2e7d32;color:#fff;}
.rb.rd{background:#b71c1c;color:#fff;}
.botbar{background:#6a0036;color:rgba(255,255,255,.7);font-size:10px;padding:3px 10px;text-align:center;flex-shrink:0;}
/* form */
.sh{background:#c2185b;color:#fff;padding:7px 12px;border-radius:4px 4px 0 0;font-size:14px;font-weight:600;}
.sb{background:#fff;border:1px solid #eee;border-top:none;border-radius:0 0 6px 6px;padding:10px;}
.fg{display:grid;grid-template-columns:1fr 1fr;gap:5px 14px;}
.fr{display:flex;align-items:center;gap:5px;}
.lb{background:#c2185b;color:#fff;padding:4px 7px;border-radius:3px;font-size:11px;font-weight:500;min-width:105px;text-align:right;white-space:nowrap;flex-shrink:0;}
.lb.p{background:#7b1fa2;}
.fi2{flex:1;padding:4px 7px;border:1px solid #ddd;border-radius:3px;font-size:12px;width:100%;}
.fi2:disabled{background:#f5f5f5;color:#999;}
.pw{position:relative;flex:1;display:flex;align-items:center;}
.pw input{flex:1;padding:4px 26px 4px 7px;border:1px solid #ddd;border-radius:3px;font-size:12px;width:100%;}
.pw input:disabled{background:#f5f5f5;}
.eye{position:absolute;right:5px;cursor:pointer;font-size:13px;color:#c2185b;background:none;border:none;}
/* tables */
.dt{width:100%;border-collapse:collapse;font-size:11px;}
.dt th{background:#c2185b;color:#fff;padding:5px 7px;text-align:left;font-weight:500;}
.dt td{padding:4px 7px;border-bottom:1px solid #eee;}
.dt tr:hover td{background:#fce4ec;}
/* modal */
.ov{display:none;position:fixed;inset:0;background:rgba(0,0,0,.55);z-index:200;align-items:center;justify-content:center;}
.ov.open{display:flex;}
.modal{background:#fff;border-radius:8px;padding:20px;width:440px;max-height:92vh;overflow-y:auto;box-shadow:0 10px 40px rgba(0,0,0,.3);}
.modal h3{color:#c2185b;font-size:16px;margin-bottom:14px;}
.modal input,.modal select,.modal textarea{width:100%;padding:7px 10px;border:1px solid #ddd;border-radius:4px;margin-bottom:9px;font-size:13px;font-family:inherit;}
.modal textarea{resize:vertical;min-height:70px;}
.mbtns{display:flex;gap:8px;margin-top:4px;}
.mbtns button{flex:1;padding:8px;border:none;border-radius:4px;font-size:13px;cursor:pointer;font-weight:600;}
.bsave{background:#c2185b;color:#fff;}
.bdel{background:#f44336;color:#fff;}
.bcancel{background:#999;color:#fff;}
/* login */
.lcard{background:#fff;border-radius:10px;padding:32px;width:320px;box-shadow:0 8px 32px rgba(0,0,0,.25);}
.llogo{text-align:center;font-size:28px;font-weight:800;color:#c2185b;letter-spacing:2px;margin-bottom:4px;}
.lsub{text-align:center;font-size:12px;color:#999;margin-bottom:22px;}
.lcard label{display:block;font-size:11px;color:#777;margin-bottom:3px;}
.lcard input{width:100%;padding:9px 11px;border:1px solid #ddd;border-radius:5px;margin-bottom:12px;font-size:13px;}
.lerr{color:#e53935;font-size:12px;margin-bottom:8px;display:none;}
.lcard button{width:100%;padding:10px;background:#c2185b;color:#fff;border:none;border-radius:5px;font-size:14px;font-weight:700;cursor:pointer;}
/* stat */
.sg{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin-bottom:10px;}
.sc{background:#fff;border:1px solid #eee;border-radius:6px;padding:8px 10px;text-align:center;}
.sc .n{font-size:22px;font-weight:700;}
.sc .l{font-size:11px;color:#888;margin-top:2px;}
/* ticket */
.tc{background:#fff;border:1px solid #eee;border-radius:6px;padding:10px 12px;margin-bottom:6px;cursor:pointer;}
.tc:hover{border-color:#c2185b;}
.tc.open .ts{background:#ef5350;color:#fff;}
.tc.waiting .ts{background:#ef9a26;color:#fff;}
.tc.closed .ts{background:#43a047;color:#fff;}
.ts{display:inline-block;padding:2px 8px;border-radius:12px;font-size:10px;font-weight:600;margin-bottom:4px;}
/* yedek rapor */
.yw{display:flex;flex-direction:column;gap:10px;}
/* mail girisi */
.mip{background:#fff;border:1px solid #ddd;border-radius:8px;overflow:hidden;}
.mih{background:#880e4f;color:#fff;padding:9px 14px;font-size:13px;font-weight:600;}
.mib{padding:12px;}
.mmr{display:flex;gap:8px;margin-bottom:8px;flex-wrap:wrap;}
.mmr input{flex:1;min-width:140px;padding:5px 9px;border:1px solid #ddd;border-radius:4px;font-size:12px;}
.mta{width:100%;height:110px;padding:8px 10px;border:1px solid #ddd;border-radius:4px;font-size:12px;font-family:'Courier New',monospace;resize:vertical;background:#fafafa;}
.mta:focus{outline:none;border-color:#880e4f;background:#fff;}
.mac{display:flex;gap:8px;margin-top:8px;align-items:center;}
.mb{padding:7px 18px;border:none;border-radius:4px;cursor:pointer;font-size:12px;font-weight:600;}
.mbp{background:#880e4f;color:#fff;}
.mbs{background:#f0f0f0;border:1px solid #ddd;color:#555;}
/* filtre */
.fb{background:#fff;border:1px solid #ddd;border-radius:8px;padding:10px 14px;display:flex;align-items:center;gap:8px;flex-wrap:wrap;}
.fb label{font-size:12px;color:#555;font-weight:500;}
.fp{padding:5px 14px;border:1px solid #ddd;border-radius:20px;background:#f5f5f5;font-size:12px;cursor:pointer;color:#555;font-family:inherit;}
.fp.active{background:#c2185b;color:#fff;border-color:#c2185b;}
/* ozet */
.og{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;}
.ok2{background:#fff;border:1px solid #eee;border-radius:8px;padding:10px 12px;text-align:center;}
.ok2.herr{border:2px solid #f44336;background:#fff8f8;}
.ok2 .on{font-size:20px;font-weight:700;}
.ok2 .ol{font-size:11px;color:#888;margin-top:2px;}
/* rapor tablo */
.rtw{background:#fff;border:1px solid #eee;border-radius:8px;overflow:hidden;}
.rth{padding:10px 14px;border-bottom:1px solid #eee;display:flex;align-items:center;justify-content:space-between;}
.rth h4{font-size:13px;font-weight:600;color:#333;}
.rt{width:100%;border-collapse:collapse;font-size:12px;}
.rt th{background:#f5f5f5;padding:7px 10px;text-align:left;font-weight:600;color:#555;border-bottom:2px solid #eee;}
.rt th.tv{border-bottom-color:#1565c0;}
.rt th.tq{border-bottom-color:#2e7d32;}
.rt th.tf{border-bottom-color:#880e4f;}
.rt td{padding:6px 10px;border-bottom:1px solid #f0f0f0;vertical-align:middle;}
.rt tr:hover td{background:#fafafa;}
.rt tr.rerr{background:#fff8f8;}
.pok{display:inline-flex;align-items:center;gap:4px;background:#e8f5e9;color:#2e7d32;padding:2px 8px;border-radius:12px;font-size:11px;font-weight:600;}
.perr{display:inline-flex;align-items:center;gap:4px;background:#ffebee;color:#c62828;padding:2px 8px;border-radius:12px;font-size:11px;font-weight:600;}
.pnone{display:inline-flex;align-items:center;gap:4px;background:#f5f5f5;color:#999;padding:2px 8px;border-radius:12px;font-size:11px;}
.dok{width:7px;height:7px;border-radius:50%;background:#43a047;}
.derr{width:7px;height:7px;border-radius:50%;background:#e53935;}
.dnone{width:7px;height:7px;border-radius:50%;background:#ccc;}
/* hata panel */
.hp{background:#fff;border:2px solid #f44336;border-radius:8px;overflow:hidden;}
.hph{background:#f44336;color:#fff;padding:8px 14px;font-size:13px;font-weight:600;}
.hi{padding:10px 14px;border-bottom:1px solid #ffebee;display:flex;align-items:flex-start;gap:10px;}
.hi:last-child{border-bottom:none;}
.hb{background:#ffebee;color:#c62828;padding:2px 8px;border-radius:3px;font-size:10px;font-weight:700;white-space:nowrap;flex-shrink:0;margin-top:2px;}
.hdb{margin-left:auto;background:none;border:1px solid #f44336;color:#f44336;padding:3px 10px;border-radius:3px;cursor:pointer;font-size:11px;flex-shrink:0;}
.hdb:hover{background:#f44336;color:#fff;}
/* mail popup */
.mpop{display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:300;align-items:center;justify-content:center;}
.mpop.open{display:flex;}
.mpin{background:#fff;border-radius:8px;padding:20px;width:620px;max-height:82vh;overflow-y:auto;}
.mpre{font-family:'Courier New',monospace;font-size:11px;background:#f5f5f5;padding:12px;border-radius:4px;white-space:pre-wrap;word-break:break-word;max-height:380px;overflow-y:auto;line-height:1.6;}
/* user */
.ur{display:flex;align-items:center;gap:8px;padding:8px 10px;background:#fff;border:1px solid #eee;border-radius:6px;margin-bottom:5px;}
.ua{width:32px;height:32px;border-radius:50%;background:#c2185b;color:#fff;display:flex;align-items:center;justify-content:center;font-size:13px;font-weight:700;flex-shrink:0;}
.rp{padding:2px 10px;border-radius:12px;font-size:10px;font-weight:600;}
.r1{background:#e3f2fd;color:#1565c0;}.r2{background:#e8f5e9;color:#2e7d32;}.r3{background:#fff3e0;color:#e65100;}.r8{background:#f3e5f5;color:#6a1b9a;}.r9{background:#fce4ec;color:#880e4f;}
.loading{text-align:center;padding:30px;color:#999;font-size:13px;}
.noacc{text-align:center;padding:40px;color:#e53935;font-size:14px;}
</style>
</head>
<body>

<div class="ov open" id="loginOvl" style="position:fixed;background:#1a1a2e;">
  <div class="lcard">
    <div class="llogo">FERBİS</div>
    <div class="lsub">K2B Bilgi Teknolojileri</div>
    <label>Kullanıcı Adı</label><input type="text" id="lUser" autocomplete="username"/>
    <label>Şifre</label><input type="password" id="lPass" autocomplete="current-password"/>
    <div class="lerr" id="lErr">Kullanıcı adı veya şifre hatalı!</div>
    <button onclick="doLogin()">Giriş Yap</button>
  </div>
</div>

<div class="ov" id="ticketMod">
  <div class="modal" style="width:500px;">
    <h3 id="tMT">Yeni Ticket</h3>
    <label style="font-size:12px;color:#777;">Başlık</label><input type="text" id="t_b"/>
    <label style="font-size:12px;color:#777;">Firma</label><input type="text" id="t_f"/>
    <label style="font-size:12px;color:#777;">Açıklama</label><textarea id="t_a"></textarea>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
      <div><label style="font-size:12px;color:#777;">Öncelik</label>
        <select id="t_o"><option>Yüksek</option><option selected>Orta</option><option>Düşük</option></select></div>
      <div><label style="font-size:12px;color:#777;">Durum</label>
        <select id="t_d"><option value="open">Açık</option><option value="waiting">Beklemede</option><option value="closed">Kapalı</option></select></div>
    </div>
    <label style="font-size:12px;color:#777;display:block;margin-top:6px;">Personel</label>
    <select id="t_p"></select>
    <div class="mbtns" style="margin-top:10px;">
      <button class="bsave" onclick="saveTicket()">💾 Kaydet</button>
      <button id="tDB" class="bdel" style="display:none;" onclick="deleteTicket()">🗑 Sil</button>
      <button class="bcancel" onclick="closeOv('ticketMod')">İptal</button>
    </div>
  </div>
</div>

<div class="ov" id="firmaMod">
  <div class="modal">
    <h3>Yeni Firma Ekle</h3>
    <label style="font-size:12px;color:#777;">Firma Ünvanı *</label><input type="text" id="fm_u"/>
    <label style="font-size:12px;color:#777;">Yetkili</label><input type="text" id="fm_y"/>
    <label style="font-size:12px;color:#777;">Telefon</label><input type="text" id="fm_t"/>
    <label style="font-size:12px;color:#777;">E-mail</label><input type="text" id="fm_m"/>
    <div class="mbtns"><button class="bsave" onclick="saveFirma()">💾 Kaydet</button><button class="bcancel" onclick="closeOv('firmaMod')">İptal</button></div>
  </div>
</div>

<div class="ov" id="kulMod">
  <div class="modal">
    <h3 id="kMT">Yeni Kullanıcı</h3>
    <label style="font-size:12px;color:#777;">Ad Soyad *</label><input type="text" id="k_as"/>
    <label style="font-size:12px;color:#777;">Kullanıcı Adı *</label><input type="text" id="k_ka"/>
    <label style="font-size:12px;color:#777;">Şifre <span id="kSH" style="color:#aaa;font-size:11px;">(boş=değişmez)</span></label>
    <div style="position:relative;"><input type="password" id="k_s" style="width:100%;padding:7px 36px 7px 10px;border:1px solid #ddd;border-radius:4px;margin-bottom:9px;font-size:13px;"/>
    <button onclick="tp('k_s')" style="position:absolute;right:8px;top:8px;background:none;border:none;cursor:pointer;color:#c2185b;font-size:14px;">👁</button></div>
    <label style="font-size:12px;color:#777;">Rol</label><select id="k_r"></select>
    <label style="font-size:12px;color:#777;margin-top:4px;display:block;">Durum</label>
    <select id="k_ak"><option value="true">Aktif</option><option value="false">Pasif</option></select>
    <div class="mbtns" style="margin-top:8px;">
      <button class="bsave" onclick="saveKul()">💾 Kaydet</button>
      <button id="kDB" class="bdel" style="display:none;" onclick="delKul()">🗑 Sil</button>
      <button class="bcancel" onclick="closeOv('kulMod')">İptal</button>
    </div>
  </div>
</div>

<div class="mpop" id="mailPop">
  <div class="mpin">
    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px;">
      <h4 id="mpT" style="color:#c2185b;font-size:15px;">Mail İçeriği</h4>
      <button onclick="document.getElementById('mailPop').classList.remove('open')" style="background:#999;color:#fff;border:none;padding:4px 14px;border-radius:4px;cursor:pointer;font-size:12px;">✕ Kapat</button>
    </div>
    <div id="mpB"></div>
  </div>
</div>

<div class="app" id="mainApp" style="display:none;">
  <div class="topbar">
    <div style="display:flex;align-items:center;gap:10px;"><span class="brand">FERBİS</span><span style="font-size:10px;opacity:.7;">K2B Bilgi Teknolojileri</span></div>
    <div class="topmenu">
      <span id="mFE" style="display:none;" onclick="openOv('firmaMod')">+ Firma Ekle</span>
      <span onclick="showStat()">İstatistik</span>
      <span id="mKU" style="display:none;" onclick="showKullar()">👥 Kullanıcılar</span>
      <span id="mFL" style="display:none;" onclick="showFLog()">📋 Log</span>
      <span onclick="showPwd()" style="background:rgba(255,255,255,.12);">🔑 Şifrem</span>
      <span onclick="doLogout()" style="background:rgba(255,255,255,.15);">Çıkış</span>
    </div>
    <div class="uinfo"><span id="tUser"></span><span class="rbadge" id="tRole"></span></div>
  </div>
  <div class="body">
    <div class="left">
      <div class="lsearch"><input type="text" id="srch" placeholder="🔍 Firma ara..." oninput="filterF()"/></div>
      <h4>Firma Ünvanı</h4>
      <div class="flist" id="fList"><div class="loading">Yükleniyor...</div></div>
      <div style="padding:6px 8px;border-top:1px solid #eee;"><div style="font-size:10px;color:#999;margin-bottom:4px;text-transform:uppercase;">Şube</div><div style="font-size:12px;padding:3px 6px;background:#880e4f;color:#fff;border-radius:3px;">1 - Merkez</div></div>
    </div>
    <div class="center">
      <div class="fbar" id="fBar">— Firma Seçiniz —</div>
      <div class="tabs">
        <div class="tab active" data-t="firewall" onclick="showTab('firewall',this)">Firewall</div>
        <div class="tab" data-t="server" onclick="showTab('server',this)">Server</div>
        <div class="tab" data-t="nas" onclick="showTab('nas',this)">NAS</div>
        <div class="tab" data-t="modem" onclick="showTab('modem',this)">Modem</div>
        <div class="tab" data-t="ap" onclick="showTab('ap',this)">AP</div>
        <div class="tab" data-t="hosting" onclick="showTab('hosting',this)">Hosting</div>
        <div class="tab" data-t="switch" onclick="showTab('switch',this)">Switch</div>
        <div class="tab" data-t="tickets" onclick="showTab('tickets',this)">🎫 Ticketlar</div>
        <div class="tab" data-t="yedek" onclick="showTab('yedek',this)">📊 Yedek Raporu</div>
        <div class="tab" data-t="not" onclick="showTab('not',this)">📝 Notlar</div>
      </div>
      <div class="content" id="con"><div class="loading">Sol listeden firma seçin</div></div>
    </div>
    <div class="right">
      <div class="rb" onclick="showTab('firewall',null)">Firewall</div>
      <div class="rb" onclick="showTab('server',null)">Server</div>
      <div class="rb" onclick="showTab('nas',null)">NAS</div>
      <div class="rb" onclick="showTab('hosting',null)">Hosting</div>
      <div class="rb" onclick="showTab('not',null)">Notlar</div>
      <div class="rb gr" onclick="showTab('ap',null)">AP</div>
      <div class="rb" onclick="showTab('modem',null)">Modem</div>
      <div class="rb" onclick="showTab('tickets',null)">🎫 Tickets</div>
      <div class="rb gr" onclick="showTab('yedek',null)">📊 Yedek</div>
      <div class="rb rd" onclick="doLogout()">KAPAT</div>
    </div>
  </div>
  <div class="botbar">Kayhan · Barış · Hüseyin · Gökhan · Doğan · Serhan · Erdal · Taygun · İlker · Kemal · Ümit · Sinan</div>
</div>

<script>
const SB_URL='https://lvwmhoyhxzkwguexfasd.supabase.co';
const SB_KEY='sb_publishable_Vi7tPt-xSFORcsW_ZaKvXA_3YbAk5xh';
const sb=supabase.createClient(SB_URL,SB_KEY);

let cFirma=null,cTab='firewall',cUser=null,cRole=null;
let eTid=null,eKid=null;
let aFirmalar=[],aRoller=[],aKullar=[],aMail=[];
let filtre='bugun',lCache={};

async function sha256(m){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(m));return Array.from(new Uint8Array(b)).map(x=>x.toString(16).padStart(2,'0')).join('');}
function e(v){return(v||'').toString().replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;');}
function g(id){const el=document.getElementById(id);return el?el.value:'';}
function n(){return new Date().toISOString();}
function tp(id){const el=document.getElementById(id);if(el)el.type=el.type==='password'?'text':'password';}
function openOv(id){document.getElementById(id).classList.add('open');}
function closeOv(id){document.getElementById(id).classList.remove('open');}
document.querySelectorAll('.ov').forEach(o=>o.addEventListener('click',ev=>{if(ev.target===o&&o.id!=='loginOvl')closeOv(o.id);}));
document.getElementById('mailPop').addEventListener('click',ev=>{if(ev.target===document.getElementById('mailPop'))document.getElementById('mailPop').classList.remove('open');});

function toast(msg,c='#333'){
  let t=document.getElementById('_t');
  if(!t){t=document.createElement('div');t.id='_t';t.style.cssText='position:fixed;bottom:24px;right:24px;padding:10px 18px;border-radius:6px;font-size:13px;color:#fff;z-index:9999;transition:opacity .3s;';document.body.appendChild(t);}
  t.style.background=c;t.textContent=msg;t.style.opacity='1';clearTimeout(t._x);t._x=setTimeout(()=>t.style.opacity='0',2800);
}
function svBtn(fn){return`<div style="margin-top:10px;display:flex;gap:8px;align-items:center;"><button onclick="${fn}" style="background:#c2185b;color:#fff;border:none;padding:7px 20px;border-radius:4px;cursor:pointer;font-size:13px;font-weight:600;">💾 Kaydet</button><span id="sm" style="font-size:11px;"></span></div>`;}
function sm(msg,c){const el=document.getElementById('sm');if(el){el.textContent=msg;el.style.color=c||'#43a047';setTimeout(()=>{if(el)el.textContent='';},3000);}}
function fld(lb,vl,id,pw,pp,dis){
  const l=`<span class="lb${pp?' p':''}">${lb}</span>`,d=dis?'disabled':'';
  if(pw)return`<div class="fr">${l}<div class="pw"><input type="password" id="${id}" value="${e(vl)}" ${d}/><button class="eye" onclick="tp('${id}')" ${d?'style="opacity:.4"':''}>👁</button></div></div>`;
  return`<div class="fr">${l}<input class="fi2" id="${id}" value="${e(vl)}" ${d}/></div>`;
}
function can(p){if(!cRole)return false;if(cRole.seviye>=9)return true;return!!cRole[p];}
function canV(s){return can(s+'_goruntule');}
function canE(s){return can(s+'_duzenle');}

function applyTabs(){
  const m={firewall:'firewall',server:'server',nas:'nas',modem:'modem',ap:'ap',hosting:'hosting',tickets:'ticket',yedek:'yedek'};
  document.querySelectorAll('.tab[data-t]').forEach(t=>{const k=m[t.dataset.t];if(k&&!canV(k))t.classList.add('off');else t.classList.remove('off');});
  if(can('firma_ekle'))document.getElementById('mFE').style.display='';
  if(can('kullanici_yonet')){document.getElementById('mKU').style.display='';document.getElementById('mFL').style.display='';}
}

async function doLogin(){
  const u=document.getElementById('lUser').value.trim(),p=document.getElementById('lPass').value;
  if(!u||!p)return;
  const h=await sha256(p);
  const{data}=await sb.from('kullanicilar').select('*,rol:roller(*)').eq('kullanici_adi',u).eq('aktif',true).maybeSingle();
  if(!data){document.getElementById('lErr').style.display='block';return;}
  const ok=data.sifre_hash?data.sifre_hash===h:data.sifre===p;
  if(!ok){document.getElementById('lErr').style.display='block';return;}
  if(!data.sifre_hash)await sb.from('kullanicilar').update({sifre_hash:h,sifre:null}).eq('id',data.id);
  await sb.from('kullanicilar').update({son_giris:n()}).eq('id',data.id);
  cUser=data;cRole=data.rol||{seviye:1};
  document.getElementById('loginOvl').classList.remove('open');
  document.getElementById('mainApp').style.display='flex';
  document.getElementById('tUser').textContent=data.ad_soyad||u;
  document.getElementById('tRole').textContent=cRole.rol_adi||data.rol;
  applyTabs();
  await Promise.all([loadFirmalar(),loadKullar(),loadRoller(),loadMailDef()]);
  await fLog('Giriş yapıldı','kullanicilar',null,null,'Sistem girişi');
}
document.getElementById('lPass').addEventListener('keydown',ev=>{if(ev.key==='Enter')doLogin();});

async function doLogout(){
  await fLog('Çıkış yapıldı','kullanicilar',null,null,'');
  document.getElementById('mainApp').style.display='none';
  document.getElementById('loginOvl').classList.add('open');
  ['lUser','lPass'].forEach(id=>document.getElementById(id).value='');
  document.getElementById('lErr').style.display='none';
  cFirma=null;cUser=null;cRole=null;aFirmalar=[];
  ['mFE','mKU','mFL'].forEach(id=>document.getElementById(id).style.display='none');
}

async function fLog(islem,tablo,fid,fadi,detay){
  if(!cUser)return;
  try{await sb.from('faaliyet_log').insert({kullanici_id:cUser.id,kullanici_adi:cUser.ad_soyad||cUser.kullanici_adi,islem,tablo,firma_id:fid||null,firma_adi:fadi||null,detay:detay||null});}catch(x){}
}

async function loadFirmalar(){const{data}=await sb.from('firmalar').select('id,unvan').order('unvan');aFirmalar=data||[];renderFL(aFirmalar);}
function filterF(){const q=document.getElementById('srch').value.toLowerCase();renderFL(aFirmalar.filter(f=>f.unvan.toLowerCase().includes(q)));}
function renderFL(list){
  const el=document.getElementById('fList');
  if(!list.length){el.innerHTML='<div style="padding:10px;font-size:12px;color:#aaa;">Bulunamadı</div>';return;}
  el.innerHTML=list.map(f=>`<div class="fi${cFirma&&cFirma.id===f.id?' active':''}" onclick="selF('${f.id}','${e(f.unvan)}')">${e(f.unvan)}</div>`).join('');
}
async function selF(id,unvan){
  cFirma={id,unvan};document.getElementById('fBar').textContent=unvan;
  const q=document.getElementById('srch').value.toLowerCase();
  renderFL(aFirmalar.filter(f=>f.unvan.toLowerCase().includes(q)));
  await renderTab(cTab);
}
async function saveFirma(){
  if(!can('firma_ekle')){toast('Yetkiniz yok!','#e53935');return;}
  const unvan=g('fm_u').trim();if(!unvan){alert('Firma ünvanı boş!');return;}
  const{data,error}=await sb.from('firmalar').insert({unvan,yetkili:g('fm_y'),tel:g('fm_t'),mail:g('fm_m')}).select().single();
  if(error){alert('Hata: '+error.message);return;}
  closeOv('firmaMod');['fm_u','fm_y','fm_t','fm_m'].forEach(id=>document.getElementById(id).value='');
  await fLog('Firma eklendi','firmalar',data.id,unvan);
  await loadFirmalar();selF(data.id,data.unvan);toast('Firma eklendi!','#43a047');
}

function showTab(tab,el){
  cTab=tab;
  document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));
  if(el)el.classList.add('active');else document.querySelectorAll('.tab').forEach(t=>{if(t.dataset.t===tab)t.classList.add('active');});
  renderTab(tab);
}
async function renderTab(tab){
  const a=document.getElementById('con');
  if(!cFirma&&tab!=='tickets'&&tab!=='yedek'){a.innerHTML='<div class="loading">Sol listeden firma seçin</div>';return;}
  a.innerHTML='<div class="loading">Yükleniyor...</div>';
  if(tab==='firewall')await rFW();
  else if(tab==='server')await rSV();
  else if(tab==='nas')await rNAS();
  else if(tab==='modem')await rMOD();
  else if(tab==='ap')await rAP();
  else if(tab==='hosting')await rHOS();
  else if(tab==='switch')await rSW();
  else if(tab==='not')await rNOT();
  else if(tab==='tickets')await rTK();
  else if(tab==='yedek')await rYEDEK();
}

async function ups(table,payload,fid,islem){
  const{data:ex}=await sb.from(table).select('id').eq('firma_id',fid).maybeSingle();
  let err;
  if(ex)({error:err}=await sb.from(table).update(payload).eq('id',ex.id));
  else({error:err}=await sb.from(table).insert(payload));
  if(!err)await fLog(islem||table+' güncellendi',table,fid,cFirma?.unvan);
  sm(err?'✗ Hata: '+err.message:'✔ Kaydedildi!',err?'#e53935':'#43a047');
}

async function rFW(){
  if(!canV('firewall')){document.getElementById('con').innerHTML='<div class="noacc">Görüntüleme yetkiniz yok.</div>';return;}
  const{data}=await sb.from('firewall').select('*').eq('firma_id',cFirma.id).maybeSingle();
  const fw=data||{},d=!canE('firewall');
  document.getElementById('con').innerHTML=`<div class="sh">Firewall Bilgileri${d?' <span style="font-size:11px;opacity:.7;">(Salt Okunur)</span>':''}</div><div class="sb">
<div style="display:flex;gap:4px;margin-bottom:8px;flex-wrap:wrap;">
<span style="padding:4px 10px;border-radius:20px;background:#c2185b;color:#fff;font-size:11px;">Wan 1</span>
<span style="padding:4px 10px;border-radius:20px;background:#e0e0e0;color:#555;font-size:11px;">Wan 2</span>
<span style="padding:4px 10px;border-radius:20px;background:#e0e0e0;color:#555;font-size:11px;">Wan 3</span>
<span style="padding:4px 10px;border-radius:20px;background:#e0e0e0;color:#555;font-size:11px;">Wan 4</span></div>
<div class="fg">
${fld('Marka',fw.marka,'fw_mk',false,false,d)}${fld('WAN 1',fw.wan1,'fw_w1',false,true,d)}
${fld('Model',fw.model,'fw_mo',false,false,d)}${fld('WAN 2',fw.wan2,'fw_w2',false,true,d)}
${fld('Seri No',fw.seri_no,'fw_sn',false,false,d)}${fld('WAN 3',fw.wan3,'fw_w3',false,true,d)}
${fld('Versiyon',fw.versiyon,'fw_vr',false,false,d)}${fld('WAN 4',fw.wan4,'fw_w4',false,true,d)}
${fld('IP Adresi',fw.ip,'fw_ip',false,false,d)}${fld('Register',fw.register,'fw_rg',false,true,d)}
${fld('Bağlantı Portu',fw.port,'fw_pt',false,false,d)}${fld('Lisans Tarihi',fw.lisans,'fw_ls',false,true,d)}
${fld('Kullanıcı Adı',fw.kullanici,'fw_ku',false,false,d)}${fld('Hostname',fw.hostname,'fw_hn',false,true,d)}
${fld('Şifresi',fw.sifre,'fw_sf',true,false,d)}
</div>${d?'':svBtn('sFW()')}</div>`;
}
async function sFW(){if(!canE('firewall')){toast('Yetkiniz yok!','#e53935');return;}
  await ups('firewall',{firma_id:cFirma.id,marka:g('fw_mk'),model:g('fw_mo'),seri_no:g('fw_sn'),versiyon:g('fw_vr'),ip:g('fw_ip'),port:g('fw_pt'),kullanici:g('fw_ku'),sifre:g('fw_sf'),wan1:g('fw_w1'),wan2:g('fw_w2'),wan3:g('fw_w3'),wan4:g('fw_w4'),register:g('fw_rg'),lisans:g('fw_ls'),hostname:g('fw_hn'),updated_at:n()},cFirma.id,'Firewall güncellendi');}

async function rSV(){
  if(!canV('server')){document.getElementById('con').innerHTML='<div class="noacc">Görüntüleme yetkiniz yok.</div>';return;}
  const{data}=await sb.from('server').select('*').eq('firma_id',cFirma.id).maybeSingle();
  const sv=data||{},d=!canE('server');
  document.getElementById('con').innerHTML=`<div class="sh">Server Bilgileri</div><div class="sb"><div class="fg">
${fld('Marka',sv.marka,'sv_mk',false,false,d)}${fld('Server Adı',sv.server_adi,'sv_sa',false,true,d)}
${fld('Model',sv.model,'sv_mo',false,false,d)}${fld('İşletim Sistemi',sv.os,'sv_os',false,true,d)}
${fld('Seri No',sv.seri_no,'sv_sn',false,false,d)}${fld('Domain',sv.domain,'sv_dm',false,true,d)}
${fld('Ram',sv.ram,'sv_rm',false,false,d)}${fld('Kullanıcı Adı',sv.kullanici,'sv_ku',false,true,d)}
${fld('ILO User',sv.ilo_user,'sv_iu',false,false,d)}${fld('Şifresi',sv.sifre,'sv_sf',true,false,d)}
${fld('ILO Password',sv.ilo_pass,'sv_ip',true,false,d)}${fld('İç IP 1',sv.ic_ip1,'sv_i1',false,true,d)}
${fld('ILO Name',sv.ilo_name,'sv_in',false,false,d)}${fld('RDP İç',sv.rdp_ic,'sv_ri',false,true,d)}
${fld('ILO IP',sv.ilo_ip,'sv_ii',false,false,d)}${fld('RDP Dış',sv.rdp_dis,'sv_rd',false,true,d)}
</div>${d?'':svBtn('sSV()')}</div>`;
}
async function sSV(){if(!canE('server')){toast('Yetkiniz yok!','#e53935');return;}
  await ups('server',{firma_id:cFirma.id,marka:g('sv_mk'),model:g('sv_mo'),seri_no:g('sv_sn'),ram:g('sv_rm'),server_adi:g('sv_sa'),os:g('sv_os'),domain:g('sv_dm'),kullanici:g('sv_ku'),sifre:g('sv_sf'),ic_ip1:g('sv_i1'),rdp_ic:g('sv_ri'),rdp_dis:g('sv_rd'),ilo_user:g('sv_iu'),ilo_pass:g('sv_ip'),ilo_name:g('sv_in'),ilo_ip:g('sv_ii'),updated_at:n()},cFirma.id,'Server güncellendi');}

async function rNAS(){
  if(!canV('nas')){document.getElementById('con').innerHTML='<div class="noacc">Görüntüleme yetkiniz yok.</div>';return;}
  const{data}=await sb.from('nas').select('*').eq('firma_id',cFirma.id).maybeSingle();
  const nas=data||{},d=!canE('nas');
  document.getElementById('con').innerHTML=`<div class="sh">NAS Bilgileri</div><div class="sb"><div class="fg">
${fld('Marka',nas.marka,'ns_mk',false,false,d)}${fld('ISCSI Alan',nas.iscsi,'ns_is',false,true,d)}
${fld('Model',nas.model,'ns_mo',false,false,d)}${fld('Disk Sayısı',nas.disk_sayisi,'ns_ds',false,true,d)}
${fld('Kullanıcı Adı',nas.kullanici,'ns_ku',false,false,d)}${fld('Toplam Kapasite',nas.top_kapasite,'ns_tk',false,true,d)}
${fld('Şifre',nas.sifre,'ns_sf',true,false,d)}${fld('IP Adresi 1',nas.ip1,'ns_i1',false,true,d)}
${fld('İç Port',nas.ic_port,'ns_ic',false,false,d)}${fld('Dış Port',nas.dis_port,'ns_dp',false,true,d)}
${fld('Mail Adresi',nas.mail_adresi,'ns_ma',false,false,d)}${fld('USB 1',nas.usb1,'ns_u1',false,true,d)}
</div>${d?'':svBtn('sNAS()')}</div>`;
}
async function sNAS(){if(!canE('nas')){toast('Yetkiniz yok!','#e53935');return;}
  await ups('nas',{firma_id:cFirma.id,marka:g('ns_mk'),model:g('ns_mo'),kullanici:g('ns_ku'),sifre:g('ns_sf'),ip1:g('ns_i1'),ic_port:g('ns_ic'),dis_port:g('ns_dp'),iscsi:g('ns_is'),disk_sayisi:g('ns_ds'),top_kapasite:g('ns_tk'),mail_adresi:g('ns_ma'),usb1:g('ns_u1'),updated_at:n()},cFirma.id,'NAS güncellendi');}

async function rMOD(){
  const{data}=await sb.from('modem').select('*').eq('firma_id',cFirma.id).maybeSingle();
  const m=data||{},d=!canE('modem');
  document.getElementById('con').innerHTML=`<div class="sh">Modem Bilgileri</div><div class="sb"><div class="fg">
${fld('Marka',m.marka,'md_mk',false,false,d)}${fld('Kullanıcı Adı',m.kullanici,'md_ku',false,true,d)}
${fld('Model',m.model,'md_mo',false,false,d)}${fld('Şifresi',m.sifre,'md_sf',true,false,d)}
${fld('IP Adres',m.ip,'md_ip',false,false,d)}${fld('ADSL Kull. Adı',m.adsl_kull,'md_ak',false,true,d)}
${fld('Port',m.port,'md_pt',false,false,d)}${fld('ADSL Şifresi',m.adsl_sifre,'md_as',true,false,d)}
</div>${d?'':svBtn('sMOD()')}</div>`;
}
async function sMOD(){await ups('modem',{firma_id:cFirma.id,marka:g('md_mk'),model:g('md_mo'),ip:g('md_ip'),port:g('md_pt'),kullanici:g('md_ku'),sifre:g('md_sf'),adsl_kull:g('md_ak'),adsl_sifre:g('md_as'),updated_at:n()},cFirma.id,'Modem güncellendi');}

async function rAP(){
  const{data}=await sb.from('access_point').select('*').eq('firma_id',cFirma.id).maybeSingle();
  const ap=data||{},d=!canE('ap');
  document.getElementById('con').innerHTML=`<div class="sh">Access Point Bilgileri</div><div class="sb"><div class="fg">
${fld('Marka',ap.marka,'ap_mk',false,false,d)}${fld('IP Adresi',ap.ip,'ap_ip',false,true,d)}
${fld('Model',ap.model,'ap_mo',false,false,d)}${fld('Kullanıcı Adı',ap.kullanici,'ap_ku',false,true,d)}
${fld('SSID 1',ap.ssid1,'ap_s1',false,false,d)}${fld('Şifresi',ap.sifre,'ap_sf',true,false,d)}
${fld('WIFI 1 Şifresi',ap.wifi1,'ap_w1',true,false,d)}${fld('Açıklama',ap.aciklama,'ap_ac',false,true,d)}
</div>${d?'':svBtn('sAP()')}</div>`;
}
async function sAP(){await ups('access_point',{firma_id:cFirma.id,marka:g('ap_mk'),model:g('ap_mo'),ssid1:g('ap_s1'),wifi1:g('ap_w1'),ip:g('ap_ip'),kullanici:g('ap_ku'),sifre:g('ap_sf'),aciklama:g('ap_ac'),updated_at:n()},cFirma.id,'AP güncellendi');}

async function rHOS(){
  if(!canV('hosting')){document.getElementById('con').innerHTML='<div class="noacc">Görüntüleme yetkiniz yok.</div>';return;}
  const{data}=await sb.from('hosting').select('*').eq('firma_id',cFirma.id).maybeSingle();
  const h=data||{},d=!canE('hosting');
  document.getElementById('con').innerHTML=`<div class="sh">Hosting Bilgileri</div><div class="sb"><div style="max-width:480px;display:flex;flex-direction:column;gap:6px;">
${fld('Admin',h.admin,'hs_ad',false,false,d)}${fld('Pass',h.pass,'hs_ps',true,false,d)}
${fld('Adres',h.adres,'hs_ar',false,false,d)}${fld('Domain',h.domain,'hs_dm',false,false,d)}
${fld('Firma',h.firma,'hs_fi',false,false,d)}
</div>${d?'':svBtn('sHOS()')}</div>`;
}
async function sHOS(){if(!canE('hosting')){toast('Yetkiniz yok!','#e53935');return;}await ups('hosting',{firma_id:cFirma.id,admin:g('hs_ad'),pass:g('hs_ps'),adres:g('hs_ar'),domain:g('hs_dm'),firma:g('hs_fi'),updated_at:n()},cFirma.id,'Hosting güncellendi');}

async function rSW(){
  const{data}=await sb.from('switch_bilgi').select('*').eq('firma_id',cFirma.id).maybeSingle();
  const sw=data||{};
  document.getElementById('con').innerHTML=`<div class="sh">Switch Bilgileri</div><div class="sb"><div class="fg">
${fld('Marka',sw.marka,'sw_mk')}${fld('Kullanıcı Adı',sw.kullanici,'sw_ku',false,true)}
${fld('Model',sw.model,'sw_mo')}${fld('Şifresi',sw.sifre,'sw_sf',true)}
${fld('IP Adresi',sw.ip,'sw_ip')}${fld('Port',sw.port,'sw_pt',false,true)}
</div>${svBtn('sSW()')}</div>`;
}
async function sSW(){await ups('switch_bilgi',{firma_id:cFirma.id,marka:g('sw_mk'),model:g('sw_mo'),ip:g('sw_ip'),port:g('sw_pt'),kullanici:g('sw_ku'),sifre:g('sw_sf'),updated_at:n()},cFirma.id,'Switch güncellendi');}

async function rNOT(){
  if(!cFirma){document.getElementById('con').innerHTML='<div class="loading">Sol listeden firma seçin</div>';return;}
  const{data}=await sb.from('firma_not').select('*').eq('firma_id',cFirma.id).maybeSingle();
  document.getElementById('con').innerHTML=`<div class="sh">Firma Notları</div><div class="sb">
<textarea id="notT" style="width:100%;height:220px;padding:10px;border:1px solid #ddd;border-radius:4px;font-size:13px;resize:vertical;font-family:inherit;" placeholder="Notlarınızı buraya girin...">${e(data?.not_metni||'')}</textarea>
${svBtn('sNOT()')}</div>`;
}
async function sNOT(){
  const pl={firma_id:cFirma.id,not_metni:g('notT'),updated_at:n()};
  const{data:ex}=await sb.from('firma_not').select('id').eq('firma_id',cFirma.id).maybeSingle();
  let err;if(ex)({error:err}=await sb.from('firma_not').update(pl).eq('id',ex.id));else({error:err}=await sb.from('firma_not').insert(pl));
  if(!err)await fLog('Not güncellendi','firma_not',cFirma.id,cFirma.unvan);
  sm(err?'✗ Hata: '+err.message:'✔ Kaydedildi!',err?'#e53935':'#43a047');
}

// ── TİCKETLAR ─────────────────────────────────────────────
async function rTK(){
  if(!canV('ticket')){document.getElementById('con').innerHTML='<div class="noacc">Görüntüleme yetkiniz yok.</div>';return;}
  let q=sb.from('ticketlar').select('*').order('created_at',{ascending:false});
  if(cFirma)q=q.eq('firma_id',cFirma.id);
  const{data:tl}=await q;const list=tl||[];
  const op=list.filter(t=>t.durum==='open').length,wt=list.filter(t=>t.durum==='waiting').length,cl=list.filter(t=>t.durum==='closed').length;
  const cards=list.length?list.map(t=>`<div class="tc ${t.durum}" onclick="eTK('${t.id}','${e(t.baslik)}','${e(t.firma_adi)}','${e(t.aciklama)}','${t.oncelik}','${t.durum}','${e(t.personel)}')">
<div style="display:flex;justify-content:space-between;"><span class="ts">${t.durum==='open'?'Açık':t.durum==='waiting'?'Beklemede':'Kapalı'}</span>
<span style="font-size:11px;color:${t.oncelik==='Yüksek'?'#c62828':t.oncelik==='Orta'?'#e65100':'#2e7d32'};font-weight:600;">${t.oncelik}</span></div>
<div style="font-size:13px;font-weight:600;color:#333;margin-bottom:2px;">${e(t.baslik)}</div>
<div style="font-size:11px;color:#888;">${e(t.firma_adi||'')} · ${e(t.personel||'')} · ${new Date(t.created_at).toLocaleString('tr-TR')}</div>
<div style="font-size:11px;color:#aaa;margin-top:2px;">${e(t.aciklama||'')}</div></div>`).join(''):'<div style="text-align:center;color:#bbb;padding:30px;">Henüz ticket yok</div>';
  document.getElementById('con').innerHTML=`
<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:10px;">
<div style="font-size:15px;font-weight:600;color:#333;">${cFirma?cFirma.unvan+' — ':'Tüm '}Ticketlar</div>
${canE('ticket')?'<button onclick="nTK()" style="background:#c2185b;color:#fff;border:none;padding:7px 16px;border-radius:4px;cursor:pointer;font-size:13px;font-weight:600;">➕ Yeni Ticket</button>':''}</div>
<div class="sg"><div class="sc"><div class="n" style="color:#e53935;">${op}</div><div class="l">Açık</div></div>
<div class="sc"><div class="n" style="color:#ef9a26;">${wt}</div><div class="l">Beklemede</div></div>
<div class="sc"><div class="n" style="color:#43a047;">${cl}</div><div class="l">Kapalı</div></div></div>
<div>${cards}</div>`;
  const sel=document.getElementById('t_p');if(sel&&aKullar.length)sel.innerHTML=aKullar.map(k=>`<option>${e(k.ad_soyad||k.kullanici_adi)}</option>`).join('');
}
function nTK(){eTid=null;document.getElementById('tMT').textContent='Yeni Ticket';['t_b','t_a'].forEach(id=>document.getElementById(id).value='');document.getElementById('t_f').value=cFirma?.unvan||'';document.getElementById('t_o').value='Orta';document.getElementById('t_d').value='open';document.getElementById('tDB').style.display='none';openOv('ticketMod');}
function eTK(id,b,f,a,o,d,p){if(!canE('ticket')){toast('Düzenleme yetkiniz yok!','#e53935');return;}eTid=id;document.getElementById('tMT').textContent='Ticket Düzenle';document.getElementById('t_b').value=b;document.getElementById('t_f').value=f;document.getElementById('t_a').value=a;document.getElementById('t_o').value=o;document.getElementById('t_d').value=d;const sel=document.getElementById('t_p');if(sel)sel.value=p;document.getElementById('tDB').style.display='block';openOv('ticketMod');}
async function saveTicket(){
  const fa=g('t_f');const{data:fr}=await sb.from('firmalar').select('id').eq('unvan',fa).maybeSingle();
  const pl={firma_id:fr?.id||cFirma?.id||null,firma_adi:fa,baslik:g('t_b'),aciklama:g('t_a'),oncelik:document.getElementById('t_o').value,durum:document.getElementById('t_d').value,personel:document.getElementById('t_p').value,updated_at:n()};
  let err;if(eTid)({error:err}=await sb.from('ticketlar').update(pl).eq('id',eTid));else({error:err}=await sb.from('ticketlar').insert(pl));
  if(err){alert('Hata: '+err.message);return;}
  await fLog(eTid?'Ticket güncellendi':'Ticket oluşturuldu','ticketlar',fr?.id||cFirma?.id,fa,pl.baslik);
  closeOv('ticketMod');await rTK();toast('Ticket kaydedildi!','#43a047');
}
async function deleteTicket(){if(!eTid||!confirm('Bu ticket silinecek!'))return;await sb.from('ticketlar').delete().eq('id',eTid);closeOv('ticketMod');await rTK();toast('Ticket silindi.','#e53935');}

// ══════════════════════════════════════════════════════════
// YEDEK RAPORU
// ══════════════════════════════════════════════════════════
async function loadMailDef(){
  try{const{data}=await sb.from('mail_tanimi').select('*').eq('aktif',true);aMail=data||[];}catch(x){aMail=[];}
}

function tAralik(f){
  if(f==='tum')return null;
  const now=new Date();
  const b=new Date(now.getFullYear(),now.getMonth(),now.getDate(),0,0,0,0);
  if(f==='bugun')return b.toISOString();
  if(f==='hafta'){b.setDate(b.getDate()-7);return b.toISOString();}
  if(f==='ay'){b.setDate(1);return b.toISOString();}
  return null;
}

async function rYEDEK(){
  document.getElementById('con').innerHTML=`<div class="yw">
<!-- MAİL GİRİŞİ -->
<div class="mip">
  <div class="mih">📧 Mail Yapıştır — Otomatik Analiz ve Kayıt</div>
  <div class="mib">
    <div class="mmr">
      <input type="text" id="mk" placeholder="Konu satırı (opsiyonel)"/>
      <input type="text" id="mg" placeholder="Gönderen adres (opsiyonel)"/>
      <input type="text" id="mf" value="${cFirma?.unvan||''}" placeholder="Firma adı (opsiyonel)"/>
    </div>
    <textarea class="mta" id="mt" placeholder="Mail içeriğini buraya yapıştırın...&#10;&#10;Sistem otomatik olarak tespit eder:&#10;  • [Success] / [Failed] → başarı durumu&#10;  • Veeam · QNAP · Firewall tipi&#10;  • Tarih ve saat bilgileri&#10;  • Job adları"></textarea>
    <div class="mac">
      <button class="mb mbp" onclick="parseMail()">🔍 Analiz Et ve Kaydet</button>
      <button class="mb mbs" onclick="clearMail()">✕ Temizle</button>
      <span id="pi" style="font-size:12px;color:#43a047;font-weight:500;"></span>
    </div>
  </div>
</div>
<!-- FİLTRE -->
<div class="fb">
  <label>Dönem:</label>
  <button class="fp active" id="fb_bugun" onclick="setF('bugun')">Bugün</button>
  <button class="fp" id="fb_hafta" onclick="setF('hafta')">Bu Hafta</button>
  <button class="fp" id="fb_ay" onclick="setF('ay')">Bu Ay</button>
  <button class="fp" id="fb_tum" onclick="setF('tum')">Tümü</button>
  <div style="margin-left:auto;display:flex;align-items:center;gap:8px;">
    <label style="color:#888;font-size:12px;">Firma:</label>
    <select id="fFil" onchange="refRap()" style="padding:5px 10px;border:1px solid #ddd;border-radius:4px;font-size:12px;">
      <option value="">Tüm Firmalar</option>
      ${aFirmalar.map(f=>`<option value="${f.id}" ${cFirma?.id===f.id?'selected':''}>${e(f.unvan)}</option>`).join('')}
    </select>
  </div>
</div>
<!-- ÖZET -->
<div class="og" id="ozet"><div class="loading" style="grid-column:1/-1;">Yükleniyor...</div></div>
<!-- HATA PANELİ -->
<div id="hPanel"></div>
<!-- RAPOR TABLOSU -->
<div class="rtw">
  <div class="rth"><h4 id="rBas">Yedek Raporu</h4><span id="rSay" style="font-size:12px;color:#888;"></span></div>
  <div style="overflow-x:auto;">
    <table class="rt">
      <thead><tr>
        <th>Firma</th>
        <th class="tv">Veeam</th>
        <th class="tq">QNAP</th>
        <th class="tf">Firewall</th>
        <th>Son Tarih</th>
        <th>Kaynak</th>
      </tr></thead>
      <tbody id="rBody"><tr><td colspan="6" style="text-align:center;color:#bbb;padding:20px;">Yükleniyor...</td></tr></tbody>
    </table>
  </div>
</div>
</div>`;
  await refRap();
}

async function setF(f){
  filtre=f;
  ['bugun','hafta','ay','tum'].forEach(k=>{const el=document.getElementById('fb_'+k);if(el)el.classList.toggle('active',k===f);});
  await refRap();
}

async function refRap(){
  const bas=tAralik(filtre);
  const fFil=document.getElementById('fFil');
  const fid=fFil?fFil.value:'';

  // Sorgu — sadece temel kolonlar, 500 hatasını önler
  let q=sb.from('yedek_log').select('id,firma_id,firma_adi,baslik,durum,tip,tarih,saat,created_at,kaynak,mail_konusu,mail_govdesi').order('created_at',{ascending:false});
  if(bas)q=q.gte('created_at',bas);
  if(fid)q=q.eq('firma_id',fid);

  const{data,error}=await q;
  if(error){
    // mail_konusu yoksa daha az kolon ile tekrar dene
    let q2=sb.from('yedek_log').select('id,firma_id,firma_adi,baslik,durum,tip,tarih,saat,created_at').order('created_at',{ascending:false});
    if(bas)q2=q2.gte('created_at',bas);
    if(fid)q2=q2.eq('firma_id',fid);
    const{data:d2}=await q2;
    renderRap(d2||[]);
    return;
  }

  // Mail gövdelerini cache'le
  (data||[]).forEach(l=>{if(l.mail_govdesi)lCache[l.id]={konu:l.mail_konusu,govde:l.mail_govdesi};});
  renderRap(data||[]);
}

function renderRap(list){
  const okC=list.filter(l=>l.durum==='ok').length;
  const errC=list.filter(l=>l.durum==='err').length;
  const vC=list.filter(l=>l.tip==='veeam').length;
  const qC=list.filter(l=>l.tip==='qnap').length;
  const fC=list.filter(l=>l.tip==='firewall').length;

  // Özet kartlar
  const oz=document.getElementById('ozet');
  if(oz)oz.innerHTML=`
<div class="ok2"><div class="on" style="color:#2e7d32;">${okC}</div><div class="ol">Başarılı</div></div>
<div class="ok2${errC>0?' herr':''}"><div class="on" style="color:#c62828;">${errC}</div><div class="ol">Hatalı</div></div>
<div class="ok2"><div class="on" style="color:#1565c0;">${vC}</div><div class="ol">Veeam</div></div>
<div class="ok2"><div class="on" style="color:#2e7d32;">${qC}</div><div class="ol">QNAP</div></div>
<div class="ok2"><div class="on" style="color:#880e4f;">${fC}</div><div class="ol">Firewall</div></div>`;

  // Hata paneli
  const hatalar=list.filter(l=>l.durum==='err');
  const hp=document.getElementById('hPanel');
  if(hp){
    if(!hatalar.length){hp.innerHTML='';}
    else{hp.innerHTML=`<div class="hp">
<div class="hph">⚠ ${hatalar.length} Sorunlu Kayıt — Analiz Gerekiyor</div>
${hatalar.map(h=>`<div class="hi">
<span class="hb">${(h.tip||'').toUpperCase()}</span>
<div style="flex:1;"><div style="font-size:13px;font-weight:600;color:#333;">${e(h.firma_adi||'Bilinmiyor')}</div>
<div style="font-size:11px;color:#888;">${e(h.baslik)} · ${h.tarih||''} ${h.saat||''}</div></div>
${lCache[h.id]?`<button class="hdb" onclick="showMail('${h.id}','${e(h.baslik)}')">📧 Mail Detayı</button>`:''}
</div>`).join('')}
</div>`;}
  }

  // Rapor tablosu — firma bazlı grupla
  const fm={};
  list.forEach(l=>{
    const k=l.firma_adi||'Bilinmiyor';
    if(!fm[k])fm[k]={veeam:[],qnap:[],firewall:[],tarih:l.tarih||''};
    if(l.tip==='veeam')fm[k].veeam.push(l);
    else if(l.tip==='qnap')fm[k].qnap.push(l);
    else fm[k].firewall.push(l);
  });

  const dl={'bugun':'Bugün','hafta':'Bu Hafta','ay':'Bu Ay','tum':'Tüm Zamanlar'}[filtre];
  const rb=document.getElementById('rBas'),rs=document.getElementById('rSay');
  if(rb)rb.textContent='Yedek Raporu — '+dl;
  if(rs)rs.textContent=Object.keys(fm).length+' firma · '+list.length+' log';

  const body=document.getElementById('rBody');
  if(!body)return;
  if(!Object.keys(fm).length){body.innerHTML=`<tr><td colspan="6" style="text-align:center;color:#bbb;padding:24px;">Bu dönemde log bulunamadı</td></tr>`;return;}

  body.innerHTML=Object.entries(fm).map(([firma,d])=>{
    const hasErr=d.veeam.some(l=>l.durum==='err')||d.qnap.some(l=>l.durum==='err')||d.firewall.some(l=>l.durum==='err');
    const src=[...new Set([...d.veeam,...d.qnap,...d.firewall].map(l=>l.kaynak||'manuel'))].join(', ');
    return`<tr class="${hasErr?'rerr':''}">
<td style="font-weight:${hasErr?'600':'400'};color:${hasErr?'#c62828':'#333'};">${e(firma)}${hasErr?' ⚠':''}</td>
<td>${pill(d.veeam)}</td>
<td>${pill(d.qnap)}</td>
<td>${pill(d.firewall)}</td>
<td style="font-size:11px;color:#888;white-space:nowrap;">${d.tarih}</td>
<td style="font-size:11px;color:#aaa;">${src}</td>
</tr>`;}).join('');
}

function pill(logs){
  if(!logs.length)return`<span class="pnone"><span class="dnone"></span>—</span>`;
  const ok=logs.filter(l=>l.durum==='ok').length,err=logs.filter(l=>l.durum==='err').length;
  if(!err)return`<span class="pok"><span class="dok"></span>${ok} başarılı</span>`;
  if(!ok)return`<span class="perr"><span class="derr"></span>${err} hatalı</span>`;
  return`<span class="pok" style="margin-right:4px;"><span class="dok"></span>${ok}</span><span class="perr"><span class="derr"></span>${err} hatalı</span>`;
}

// ── MAİL PARSE ────────────────────────────────────────────
async function parseMail(){
  const text=document.getElementById('mt').value.trim();
  if(!text){toast('Mail içeriği boş!','#e53935');return;}
  const konu=g('mk'),gonderen=g('mg'),firmaHint=g('mf');
  const tl=text.toLowerCase();

  // Tip tespiti
  let tip='veeam';
  for(const t of aMail){if(tl.includes(t.anahtar_kelime.toLowerCase())){tip=t.tip;break;}}
  if(tl.includes('qnap')||tl.includes('nas backup'))tip='qnap';
  if(tl.includes('fortigate')||tl.includes('fortinet')||tl.includes('firewall alert'))tip='firewall';

  const lines=text.split('\n').map(l=>l.trim()).filter(Boolean);
  const jobLines=lines.filter(l=>/\[(success|failed|warning|error)\]/i.test(l));
  const tarihM=text.match(/(\d{1,2}[./]\d{1,2}[./]\d{2,4})/);
  const saatM=text.match(/(\d{1,2}:\d{2}(?::\d{2})?)/);
  const tarih=tarihM?tarihM[1]:new Date().toLocaleDateString('tr-TR');
  const saat=saatM?saatM[1]:new Date().toLocaleTimeString('tr-TR');
  const firmaAdi=firmaHint||tahminF(text)||'Bilinmiyor';

  const toSave=[];
  if(jobLines.length>0){
    jobLines.forEach(line=>{
      toSave.push({baslik:line.slice(0,200),tip,durum:/\[success\]/i.test(line)?'ok':'err',tarih,saat,firma_adi:firmaAdi,konu,govde:text,gonderen});
    });
  } else {
    const durum=/success|başarılı|tamamlandı|completed/i.test(text)&&!/fail|hata|error/i.test(text)?'ok':'err';
    toSave.push({baslik:(konu||lines[0]||'Mail log').slice(0,200),tip,durum,tarih,saat,firma_adi:firmaAdi,konu,govde:text,gonderen});
  }

  let saved=0,errors=0;
  for(const log of toSave){
    const{data:fr}=await sb.from('firmalar').select('id').eq('unvan',log.firma_adi).maybeSingle();
    // Temel payload — her zaman çalışır
    const ins={firma_id:fr?.id||cFirma?.id||null,firma_adi:log.firma_adi,baslik:log.baslik,durum:log.durum,tip:log.tip,tarih:log.tarih,saat:log.saat};
    // Opsiyonel kolonlar — varsa ekle
    try{ins.kaynak='mail_parse';}catch(x){}
    try{ins.mail_konusu=log.konu||null;}catch(x){}
    try{ins.mail_govdesi=log.govde||null;}catch(x){}
    try{ins.mail_gonderen=log.gonderen||null;}catch(x){}
    const{error}=await sb.from('yedek_log').insert(ins);
    if(error){
      // Opsiyonel kolonlar olmadan tekrar dene
      const ins2={firma_id:fr?.id||cFirma?.id||null,firma_adi:log.firma_adi,baslik:log.baslik,durum:log.durum,tip:log.tip,tarih:log.tarih,saat:log.saat};
      const{error:e2}=await sb.from('yedek_log').insert(ins2);
      if(e2)errors++;else saved++;
    } else saved++;
  }

  await fLog('Mail parse','yedek_log',cFirma?.id,firmaAdi,saved+' log');
  const pi=document.getElementById('pi');
  if(pi)pi.textContent='✔ '+saved+' log kaydedildi'+(errors?', '+errors+' hata':'');
  document.getElementById('mt').value='';
  toast(saved+' log kaydedildi!',errors?'#ef9a26':'#43a047');
  await refRap();
}

function tahminF(text){
  for(const f of aFirmalar){
    const k=f.unvan.split(' ')[0].toLowerCase();
    if(k.length>3&&text.toLowerCase().includes(k))return f.unvan;
  }
  return cFirma?.unvan||'';
}

function clearMail(){document.getElementById('mt').value='';const pi=document.getElementById('pi');if(pi)pi.textContent='';}

function showMail(id,baslik){
  const c=lCache[id];if(!c)return;
  document.getElementById('mpT').textContent=(baslik||'Mail').slice(0,80);
  document.getElementById('mpB').innerHTML=`${c.konu?`<div style="margin-bottom:8px;font-size:12px;"><b>Konu:</b> ${e(c.konu)}</div>`:''}
<div style="margin-bottom:6px;font-size:12px;font-weight:600;">Mail Gövdesi:</div>
<div class="mpre">${e(c.govde)}</div>`;
  document.getElementById('mailPop').classList.add('open');
}

// ── KULLANICI YÖNETİMİ ─────────────────────────────────
async function loadKullar(){const{data}=await sb.from('kullanicilar').select('*,rol:roller(*)').order('ad_soyad');aKullar=data||[];}
async function loadRoller(){const{data}=await sb.from('roller').select('*').order('seviye');aRoller=data||[];}

async function showKullar(){
  if(!can('kullanici_yonet')){toast('Yetkiniz yok!','#e53935');return;}
  cFirma=null;document.getElementById('fBar').textContent='Kullanıcı Yönetimi';
  await loadKullar();
  const rk={1:'r1',2:'r2',3:'r3',8:'r8',9:'r9'};
  document.getElementById('con').innerHTML=`
<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;">
<div style="font-size:15px;font-weight:600;color:#333;">Kullanıcılar (${aKullar.length})</div>
<button onclick="nKul()" style="background:#c2185b;color:#fff;border:none;padding:7px 16px;border-radius:4px;cursor:pointer;font-size:13px;font-weight:600;">➕ Yeni Kullanıcı</button></div>
${aKullar.map(k=>{
  const ini=(k.ad_soyad||k.kullanici_adi).split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase();
  return`<div class="ur"><div class="ua">${ini}</div>
<div style="flex:1;"><div style="font-size:13px;font-weight:600;color:#333;">${e(k.ad_soyad||k.kullanici_adi)}</div>
<div style="font-size:11px;color:#888;">${e(k.kullanici_adi)} · Son giriş: ${k.son_giris?new Date(k.son_giris).toLocaleString('tr-TR'):'-'}</div></div>
<span class="rp ${rk[k.yetki_seviye]||'r1'}">${e(k.rol?.rol_adi||k.rol||'-')}</span>
<span style="font-size:11px;padding:2px 8px;border-radius:10px;background:${k.aktif?'#e8f5e9':'#ffebee'};color:${k.aktif?'#2e7d32':'#c62828'};">${k.aktif?'Aktif':'Pasif'}</span>
<button onclick="edKul('${k.id}')" style="background:#c2185b;color:#fff;border:none;padding:4px 12px;border-radius:3px;cursor:pointer;font-size:11px;">Düzenle</button>
</div>`;}).join('')}`;
}
function nKul(){eKid=null;document.getElementById('kMT').textContent='Yeni Kullanıcı';['k_as','k_ka','k_s'].forEach(id=>document.getElementById(id).value='');document.getElementById('k_ak').value='true';document.getElementById('kSH').style.display='none';document.getElementById('kDB').style.display='none';document.getElementById('k_r').innerHTML=aRoller.map(r=>`<option value="${r.id}">${e(r.rol_adi)} (Seviye ${r.seviye})</option>`).join('');openOv('kulMod');}
function edKul(id){const k=aKullar.find(x=>x.id===id);if(!k)return;eKid=id;document.getElementById('kMT').textContent='Kullanıcı Düzenle';document.getElementById('k_as').value=k.ad_soyad||'';document.getElementById('k_ka').value=k.kullanici_adi;document.getElementById('k_s').value='';document.getElementById('k_ak').value=k.aktif?'true':'false';document.getElementById('kSH').style.display='';document.getElementById('kDB').style.display=k.id===cUser.id?'none':'block';document.getElementById('k_r').innerHTML=aRoller.map(r=>`<option value="${r.id}" ${r.id===k.rol_id?'selected':''}>${e(r.rol_adi)} (Seviye ${r.seviye})</option>`).join('');openOv('kulMod');}
async function saveKul(){
  const ka=g('k_ka'),sf=g('k_s'),rid=document.getElementById('k_r').value;
  const rol=aRoller.find(r=>r.id===rid);const ak=document.getElementById('k_ak').value==='true';
  if(!ka){alert('Kullanıcı adı boş olamaz!');return;}
  let pl={kullanici_adi:ka,ad_soyad:g('k_as'),rol_id:rid,rol:rol?.rol_adi||'user',yetki_seviye:rol?.seviye||1,aktif:ak};
  if(sf){pl.sifre_hash=await sha256(sf);pl.sifre=null;}
  let err;
  if(eKid)({error:err}=await sb.from('kullanicilar').update(pl).eq('id',eKid));
  else{if(!sf){alert('Yeni kullanıcı için şifre zorunludur!');return;}({error:err}=await sb.from('kullanicilar').insert(pl));}
  if(err){alert('Hata: '+err.message);return;}
  await fLog(eKid?'Kullanıcı güncellendi':'Kullanıcı oluşturuldu','kullanicilar',null,null,ka);
  closeOv('kulMod');await loadKullar();showKullar();toast('Kullanıcı kaydedildi!','#43a047');
}
async function delKul(){if(!eKid||!confirm('Bu kullanıcı silinecek!'))return;await sb.from('kullanicilar').delete().eq('id',eKid);closeOv('kulMod');await loadKullar();showKullar();toast('Kullanıcı silindi.','#e53935');}

// ── FAALİYET LOG ──────────────────────────────────────────
async function showFLog(){
  if(!can('kullanici_yonet')){toast('Yetkiniz yok!','#e53935');return;}
  cFirma=null;document.getElementById('fBar').textContent='Faaliyet Logu';
  document.getElementById('con').innerHTML='<div class="loading">Yükleniyor...</div>';
  const{data}=await sb.from('faaliyet_log').select('*').order('created_at',{ascending:false}).limit(200);const list=data||[];
  document.getElementById('con').innerHTML=`<div style="font-size:15px;font-weight:600;color:#333;margin-bottom:10px;">Faaliyet Logu (Son 200)</div>
<table class="dt"><tr><th>Tarih/Saat</th><th>Kullanıcı</th><th>İşlem</th><th>Tablo</th><th>Firma</th><th>Detay</th></tr>
${list.map(l=>`<tr><td style="white-space:nowrap;font-size:11px;">${new Date(l.created_at).toLocaleString('tr-TR')}</td>
<td>${e(l.kullanici_adi||'')}</td>
<td><span style="background:#fce4ec;color:#880e4f;padding:2px 6px;border-radius:3px;font-size:10px;">${e(l.islem)}</span></td>
<td style="color:#888;font-size:11px;">${e(l.tablo||'')}</td><td>${e(l.firma_adi||'')}</td>
<td style="color:#888;font-size:11px;max-width:160px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${e(l.detay||'')}</td></tr>`).join('')}</table>`;
}

// ── İSTATİSTİK ────────────────────────────────────────────
async function showStat(){
  cFirma=null;document.getElementById('fBar').textContent='Sistem İstatistikleri';
  document.getElementById('con').innerHTML='<div class="loading">Yükleniyor...</div>';
  const[{count:fC},{data:tD},{data:lD},{count:kC}]=await Promise.all([
    sb.from('firmalar').select('*',{count:'exact',head:true}),
    sb.from('ticketlar').select('durum'),
    sb.from('yedek_log').select('durum'),
    sb.from('kullanicilar').select('*',{count:'exact',head:true}).eq('aktif',true)
  ]);
  const tl=tD||[],ll=lD||[];
  document.getElementById('con').innerHTML=`<div style="font-size:16px;font-weight:700;color:#c2185b;margin-bottom:14px;">Sistem İstatistikleri</div>
<div style="display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:14px;">
<div class="sc"><div class="n" style="color:#c2185b;">${fC||0}</div><div class="l">Toplam Firma</div></div>
<div class="sc"><div class="n" style="color:#1565c0;">${kC||0}</div><div class="l">Aktif Kullanıcı</div></div>
<div class="sc"><div class="n" style="color:#e53935;">${tl.filter(t=>t.durum==='open').length}</div><div class="l">Açık Ticket</div></div>
<div class="sc"><div class="n" style="color:#43a047;">${tl.filter(t=>t.durum==='closed').length}</div><div class="l">Kapalı Ticket</div></div></div>
<div style="display:grid;grid-template-columns:repeat(3,1fr);gap:10px;">
<div class="sc"><div class="n" style="color:#43a047;">${ll.filter(l=>l.durum==='ok').length}</div><div class="l">Başarılı Yedek</div></div>
<div class="sc"><div class="n" style="color:#e53935;">${ll.filter(l=>l.durum==='err').length}</div><div class="l">Hatalı Yedek</div></div>
<div class="sc"><div class="n" style="color:#555;">${ll.length}</div><div class="l">Toplam Log</div></div></div>`;
}

// ── ŞİFRE DEĞİŞTİR ───────────────────────────────────────
function showPwd(){
  cFirma=null;document.getElementById('fBar').textContent='Şifre Değiştir';
  document.getElementById('con').innerHTML=`<div class="sh">Şifremi Değiştir</div><div class="sb" style="max-width:420px;">
<div class="fr" style="margin-bottom:8px;"><span class="lb">Mevcut Şifre</span><div class="pw"><input type="password" id="pe"/><button class="eye" onclick="tp('pe')">👁</button></div></div>
<div class="fr" style="margin-bottom:8px;"><span class="lb">Yeni Şifre</span><div class="pw"><input type="password" id="py"/><button class="eye" onclick="tp('py')">👁</button></div></div>
<div class="fr" style="margin-bottom:12px;"><span class="lb">Yeni (tekrar)</span><div class="pw"><input type="password" id="py2"/><button class="eye" onclick="tp('py2')">👁</button></div></div>
${svBtn('sPwd()')}</div>`;
}
async function sPwd(){
  const eski=g('pe'),yeni=g('py'),yeni2=g('py2');
  if(!eski||!yeni){sm('Tüm alanları doldurun!','#e53935');return;}
  if(yeni!==yeni2){sm('Şifreler eşleşmiyor!','#e53935');return;}
  if(yeni.length<6){sm('En az 6 karakter!','#e53935');return;}
  const{data}=await sb.from('kullanicilar').select('sifre_hash,sifre').eq('id',cUser.id).single();
  const ok=data.sifre_hash?data.sifre_hash===await sha256(eski):data.sifre===eski;
  if(!ok){sm('Mevcut şifre yanlış!','#e53935');return;}
  const{error}=await sb.from('kullanicilar').update({sifre_hash:await sha256(yeni),sifre:null}).eq('id',cUser.id);
  if(error){sm('Hata: '+error.message,'#e53935');return;}
  await fLog('Şifre değiştirildi','kullanicilar',null,null,'');
  sm('✔ Şifre güncellendi!','#43a047');toast('Şifreniz güncellendi!','#43a047');
}
</script>
</body>
</html>
